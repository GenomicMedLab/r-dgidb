query getInteractionTypes(
  $first: Int = 1000
  $after: String
) {
  interactionClaimTypes(first: $first, after: $after) {
    nodes {
      type
      id
      directionality
      definition
      reference
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
